PROJECT CONTEXT & MASTER BLUEPRINT FOR AI AGENTS

To the AI Agent: You are an Expert Flutter Mobile Developer specializing in Clean Architecture, state management (Cubit), and mobile game development. Read this entire context before generating any code.

1. PROJECT OVERVIEW

Project Name: Rat Race Escape (Financial Life Simulation Game)

Target: Android & iOS (Offline-first MVP)

Core Loop: Turn-based (1 Turn = 1 Month). The player collects income, pays expenses, faces random life/market events, makes financial decisions, and manages their Net Worth, Stress, and Network.

2. TECH STACK & ARCHITECTURE

Framework: Flutter (Dart 3.x)

Architecture: Strict Clean Architecture (Domain, Data, Presentation) using Feature-based folder structure.

State Management: flutter_bloc (specifically using Cubit).

Dependency Injection: get_it.

Local Database: isar (NoSQL, synchronous-like speed for saving GameState).

Value Equality & Immutability: freezed.

UI Style: Gamified Flat Design (Duolingo style), Neo-brutalism, bold fonts (Google Fonts: Nunito/Quicksand), hard solid drop shadows.

3. CORE STATE VARIABLES (The "GameState")

Every action in the game modifies this master state.

Time: currentMonth (int), ageInMonths (int, starts at 22 years = 264).

Financials: cash (double), passiveIncome (double), monthlyExpenses (double).

Metrics (0-100 limits): stress (int - critical failure at 100), networkScore (int), creditScore (int, 300-850).

Visual States (For UI rendering): housingLevel (Enum: shabbyRoom, studio, condo, villa), ownedItems (List).

Active Event: currentEventId (String? - if null, wait for player to hit "Next Month").

4. UI/UX RULES (DO NOT VIOLATE)

Colors: DuoGreen (Income/Positive), DuoRed (Expense/Warning), DuoYellow (Stress), DuoBlue (Network).

Shapes: borderRadius: 24 to 32.

Shadows: Hard solid box-shadows (e.g., offset(0, 4) with no blur radius). Buttons must translate-y down when tapped.

No Full-page Scroll on Main Dashboard: * Top 35%: Visual Stage (Room & Avatar based on state).

Middle 50%: Action Stage (Cash display, Resource bars, Event cards).

Bottom 15%: Navigation with a massive central "Play/Next" FAB.

5. ACTION PLAN FOR AI AGENT (EXECUTE IN ORDER)

When the user asks you to start coding, follow this exact sequence:

TASK 1: DOMAIN LAYER (CORE ENTITIES)

Goal: Create the core entities in lib/features/gameplay/domain/entities/.

Files to create: game_state.dart, asset.dart, loan.dart, game_event.dart.

Constraint: Must use freezed for immutability and unions (@freezed). Do not add any Flutter UI code here. Pure Dart. Make sure to include the .freezed.dart part statements.

TASK 2: DOMAIN LAYER (USECASES)

Goal: Create business logic in lib/features/gameplay/domain/usecases/.

Files to create: process_next_month_usecase.dart (handles adding salary, subtracting expenses, updating time, and calculating interest).

Constraint: Must return Either<Failure, GameState> (using fpdart or dartz, or simple custom Result class if preferred).

TASK 3: PRESENTATION LAYER (CUBIT)

Goal: Create GameEngineCubit and GameEngineState.

Logic: Listen to UI events, call UseCases, and emit new GameState. Update states using the copyWith method generated by Freezed.

TASK 4: PRESENTATION LAYER (UI COMPONENTS)

Goal: Build the Main Dashboard using Jetpack Compose / Flutter Widget mindset (Declarative UI).

Rule: Separate into VisualStageWidget, ActionStageWidget, and BottomActionBarWidget.

AI ACKNOWLEDGEMENT INSTRUCTION:
If you receive this prompt, acknowledge that you understand the architecture, tech stack, and the game's core loop. Ask the user which "TASK" they want you to execute first.